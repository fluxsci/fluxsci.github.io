# Handoff — branch `polish/hero-figures-signature` (2026-09-14)

Internal note for the agent resuming this work. `plans/` is never published.

## The ask (from the owner)

1. Make the homepage's first 20–30 seconds smoother and more signature-Flux: an elegant
   entrance where the phyllotaxis mark blooms into view and the rest of the page follows.
2. Make the example project's plots look like cutting-edge, real scientific figures and
   compose them into dense, hand-composed-looking figures (not a loose grid).
3. Highlight the "only-Flux" capabilities on the homepage: figures and slides sharing one
   editing surface, semantic plots (fluxplot) and what they enable in the app.
4. Use computer control (Chrome tools) freely to see and iterate on the result.
5. Work on this branch; the owner merges to `main` and pushes after review.

## Done so far (committed on this branch)

- **13 new flagship plots** in `examples/neural-populations/plots/flagship/` (21–33), generated by
  `scripts/advanced/flagship.py` (run from the project root with
  `uv run --project scripts/advanced python scripts/advanced/flagship.py [--only 21,24]`).
  Shared journal style: Arial 5–6.5 pt, Flexoki accents, the Flux-mark hue wheel for the eight
  drift directions, direction key (29). Previews in `exports/flagship-previews/` (not in the ZIP:
  `scripts/project-exports.json` does not list them yet — decide whether to add).
- **Figure 1 (`fig-neural-populations`) recomposed** as a dense three-row plate (1440 × 920 px,
  panels a–k + the direction key) through the pinned Flux CLI: `scripts/advanced/compose-flagship.sh`.
  Old 8 panels deleted from the figure; their assets 01–08 stay registered because the slides use them.
- **Figure 2 (`fig-neural-structure`) recomposed** (1440 × 710 px, panels a–f) from the advanced
  plots 14 (three morph states), 20, 16, 18, 17 plus new 33: `scripts/advanced/compose-structure.sh`.
- Deleted the owner's **empty "Figure 3"** (`fig_mu1vwcyuwae6_4`) that `flux validate` flagged; its
  empty canvas `canvas_mu1vwcyuwae6_3` ("Example_figures") still exists (no delete-canvas verb).
  Tell the owner; restore from git if they want it back.
- `flux validate` and `validate-deck` pass. Both figures render (`render-figure … --png`).
- Project bundle builds (18.9 MB ZIP now, was 7.9 MB; well under the 40 MB budget).

**The owner's running Flux app has this example project open.** Every CLI mutation was picked up
live (journal shows `human save_fig` right after agent writes) and the `project` lock is briefly
held afterwards: retry a CLI verb after ~5 s if it reports "deferred … locked".

## Environment set up for the media refresh

- Pinned Flux checkout (revision from `flux-source.json`, `4bb72d8…`) as a git worktree at
  `~/.cache/flux-website-pin/flux` with `npm ci` done. Remove later with
  `git -C ~/flux worktree remove ~/.cache/flux-website-pin/flux`.
- CLI wrapper used for composition: `~/.cache/flux-website-pin/flux/node_modules/.bin/tsx
  ~/.cache/flux-website-pin/flux/flux-cli.ts <verb>`, run from the project dir with
  `FLUX_PROJECT=$PWD FLUX_CLIENT=agent` (a tiny `fluxpin` shell wrapper makes `FLUX_CLI` easy).
- The dev server for captures is `npm run dev` in that worktree (port 1420). It was stopped at
  handoff; restart it before `capture-media.mjs`. Brave is installed (capture default `FLUX_CHROME`).
- Port 1430 is held by a stale `node scripts/serve.mjs --port 1431` from another agent session
  (PID 88957); Playwright reuses it, and it serves the live `dist/`, so tests still work.

## Remaining work, in order

1. **Refresh derived media** (all read the project; nothing here edits authored files):
   `node scripts/refresh-flux-assets.mjs --flux-source ~/.cache/flux-website-pin/flux` (rewrites
   `fig/renders/*.svg`, the native slide demo, poster, `media/native-assets.json`), then with the
   pinned dev server on :1420: `node scripts/capture-media.mjs --flux-source ~/.cache/flux-website-pin/flux`
   (rewrites `site/assets/media/*.webp`, `media/masters/*.png`, `exports/manuscript.pdf`,
   `media/screenshots.json`, and the `width`/`height` attributes in `site/index.qmd`). The plate
   `neural-figure.webp` becomes 1800 × ~1150; the hero screenshot then shows the new Figure 1.
2. **Homepage hero entrance** (`site/index.qmd`, `site/assets/styles/site.css`, `site/assets/scripts/site.js`):
   inline the 88-dot mark as SVG circles in the hero with `--i` per circle and reuse the app's
   bloom (`src/shell/Logomark.svelte`: settle 950 ms from rotate(−26°) scale(.82); per-dot 420 ms
   bloom, 8 ms stagger, cubic-bezier(.16,1,.3,1)), then stagger eyebrow → h1 → description →
   actions → screenshot with short rises. Rules: pure CSS keyframes (works without JS), final state
   is the base style so the existing `prefers-reduced-motion` block simply disables it, headline
   visible within ~300 ms, one-shot, never loop the mark; skip on same-session revisits via
   `sessionStorage` + a class set by the inline head script.
3. **"Only in Flux" section** (new, between the principles and Paper): (a) an interactive
   semantic-plot explorer that inlines a real fluxplot SVG from the project and, on hover/click,
   names the part under the pointer (`data-role`, `data-series`, id) and restyles it — good
   candidates: `plots/flagship/31-frequency-tuning.svg` (15 KB, areas/line/points/bars) or
   `24-example-cells.svg` (185 KB, 36 panels); label it honestly as the file format inspected in
   the browser, not the app; (b) the figure↔slide "same object, two surfaces" story using
   `figure.webp` and `slides.webp` crops (both show the same heatmap/network panels); (c) the
   agent/CLI addressability with a real command such as
   `flux restyle fig-neural-populations population-mean.line --stroke '#205EA6'`.
   Publishing an SVG from `examples/` needs a copy under `site/assets/media/` (allowlist in
   `scripts/check.mjs` already permits `.svg`; add provenance only if placed in a manifest).
4. **Copy updates** in `site/index.qmd`: figure captions/alt text still say "eight-panel"; the
   plate caption and `#example-project` copy should describe the new composition; keep the Allen credit.
5. **Project docs**: `examples/neural-populations/README.md`, `paper/manuscript.qmd` prose about the
   figures ("larger heatmap", "paired-comparison"), `paper/methods.qmd` plot construction,
   `Context/Project/MISSION.qmd` deliverable ("twelve source plots"), `plots/advanced/README.md`
   (mention the flagship set + compose scripts), a `Context/NOTEBOOK.md` session-log entry
   (`flux note` verb), and add `exports/flagship-previews/*.png` to `scripts/project-exports.json`
   if they should ship.
6. **Website docs**: `README.md` and `plans/implementation-review.md` numbers (ZIP size, figure
   panels), then `npm test` (build + server checks + 4 browser projects) and visual QA at 1440,
   800 and 390 px, light and dark, with real Chrome via the claude-in-chrome tools.
7. **Flux engineering guide**: a session-log entry is already appended (uncommitted in `~/flux`,
   where another session also has uncommitted guide edits — stage only that file if committing).
8. Commit with explicit paths; push the branch for review; do not merge to `main`.

## Verification already run

- Every panel inspected as PNG at 300 dpi; both composed figures inspected at 1.5× render.
- `flux validate` ✓ (5 files), `flux validate-deck neural-populations` ✓, bundle round-trip ✓.
- Not yet run: `npm test`, browser checks, capture/refresh scripts.
